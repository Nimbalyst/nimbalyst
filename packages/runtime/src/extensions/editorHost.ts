/**
 * Runtime re-exports the shared custom editor contract from the SDK.
 */

export type {
  EditorHost,
  EditorHostProps,
  EditorMenuItem,
  DiffConfig,
  DiffResult,
} from '@nimbalyst/extension-sdk';
