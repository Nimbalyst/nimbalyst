{
  "name": "@nimbalyst/collabv3",
  "version": "0.1.55",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --ip 0.0.0.0",
    "deploy": "wrangler deploy",
    "deploy:production": "./scripts/deploy.sh patch --env production",
    "deploy:staging": "./scripts/deploy.sh patch --env staging",
    "test": "vitest run",
    "test:integration": "vitest run test/documentRoom.integration.test.ts",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@vitest/ui": "^4.0.16",
    "partysocket": "^1.0.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.6.3",
    "vitest": "^4.0.16",
    "wrangler": "^4.51.0"
  }
}
